# Desafio Classificador de nível de Herói


Objetivo:

Criar uma variável para armazenar o nome e a quantidade de experiência (XP) de um herói e exibir na tela uma mensagem com o nome e nível:

"O Herói de nome **{nome}** está no nível de **{nivel}**"


